# Checkpoints
Contains trained model weights (`.pt` files) generated during training. Each file corresponds to a specific base encoder variant (e.g., `best_bert-mini.pt`, `best_distilbert.pt`). Automatically created by the `01_train_*.py` scripts.
